package com.oracle.visualize.usecases
/*

import com.oracle.visualize.domain.models.AuthUser
import com.oracle.visualize.domain.repositories.AuthRepository
import com.oracle.visualize.domain.usecases.RegisterUseCase
import org.junit.Before
import io.mockk.mockk
import org.junit.Test

class RegisterUseCaseTest {

    private lateinit var authRepository: AuthRepository
    private lateinit var registerUseCase: RegisterUseCase

    @Before
    fun setup() {
        authRepository = mockk()
        registerUseCase = RegisterUseCase(authRepository)
    }

    @Test
    fun `If all the data is valid, it should return “Success” along with the user`() = runTest {
        // val validUser

        // coEvery { authRepository.register(any(), any(), any())} return validUser
    }

    val result = registerUseCase("Test", "test@correo.com", "Password123", "Password123")

    asser
}*/
